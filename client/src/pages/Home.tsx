import { AIChatBox, type Message } from "@/components/AIChatBox";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Skeleton } from "@/components/ui/skeleton";
import { trpc } from "@/lib/trpc";
import { confirmsNoFurtherHelp } from "@/lib/voiceSession";
import type { AgentActivity, AppointmentSlot, BookingConfirmation, CoordinatorResult, PolicyEvidence } from "@shared/novacorp";
import { ArrowUpRight, Check, ChevronRight, CircleAlert, Clock3, FileText, HeartPulse, Loader2, LockKeyhole, LogOut, MapPin, ShieldCheck, Sparkles, Stethoscope } from "lucide-react";
import React from "react";
import { useEffect, useMemo, useState } from "react";

const suggestedPrompts = [
  "What are my specialist copay and deductible details?",
  "Does my plan cover an orthopedic consultation? Show the policy evidence.",
  "Show the earliest available orthopedic appointment.",
  "Help me book an available appointment.",
  "Help me cancel my upcoming appointment.",
];

function SectionLabel({ children }: { children: React.ReactNode }) {
  return <p className="nova-label">{children}</p>;
}

function ActivityItem({ item, isLast }: { item: AgentActivity; isLast?: boolean }) {
  const isActive = item.state === "active";
  const isError = item.state === "error";
  return <div className="relative flex gap-3 pb-5 last:pb-0">
    {!isLast && <span className="absolute left-[9px] top-[19px] h-[calc(100%-5px)] border-l border-dashed border-black/20" />}
    <span className={`relative z-10 mt-0.5 grid size-5 place-items-center rounded-full border ${isError ? "border-red-800 bg-red-100 text-red-800" : isActive ? "border-[#005a48] bg-[#dff0e9] text-[#005a48]" : "border-black/25 bg-[#f7f3ec] text-black"}`}>{isActive ? <Loader2 className="size-3 animate-spin" /> : <Check className="size-3" />}</span>
    <div className="min-w-0"><div className="flex flex-wrap items-baseline gap-x-2"><p className="font-editorial text-[17px] leading-none">{item.agent}</p><span className="text-[10px] uppercase tracking-[0.17em] text-black/45">{item.action}</span></div><p className="mt-1 text-xs leading-5 text-black/60">{item.detail}</p></div>
  </div>;
}

function EvidenceCard({ evidence, hasSearched }: { evidence: PolicyEvidence[]; hasSearched: boolean }) {
  return <section className="nova-panel bg-[#efeae0]">
    <div className="flex items-start justify-between gap-3"><div><SectionLabel>Policy evidence</SectionLabel><h2 className="mt-2 font-editorial text-2xl">Evidence, cited.</h2></div><FileText className="size-5 text-[#005a48]" /></div>
    <div className="mt-6 space-y-4">
      {!hasSearched ? <p className="border-l-2 border-[#005a48] pl-4 text-xs leading-5 text-black/60">Policy citations will appear here when an insurance question is routed to the evidence specialist.</p> : evidence.length === 0 ? <div className="border-l-2 border-[#b55239] pl-4 py-1"><p className="font-editorial text-lg">No evidence retrieved</p><p className="mt-1 text-xs leading-5 text-black/60">No policy conclusion has been made for this request.</p></div> : evidence.map(item => <article key={item.id} className="border-t border-black/15 pt-4 first:border-t-0 first:pt-0"><div className="flex items-start justify-between gap-3"><p className="max-w-[75%] text-[10px] font-semibold uppercase tracking-[0.16em] text-[#005a48]">{item.document}</p><Badge variant="outline" className="rounded-none border-black/25 bg-transparent px-2 py-1 text-[10px] font-normal tracking-wide">{Math.round(item.relevance * 100)}% relevant</Badge></div><p className="mt-2 text-sm leading-6 text-black/75">“{item.excerpt}”</p><p className="mt-3 text-[10px] uppercase tracking-[0.12em] text-black/45">{item.section} · p. {item.page}</p></article>)}
    </div>
  </section>;
}

export function MemberAccess({ onVerified, onVoiceSessionStart = () => undefined }: { onVerified: () => void; onVoiceSessionStart?: () => void }) {
  const beginConversation = trpc.care.beginVerificationConversation.useQuery();
  const [conversation, setConversation] = useState<Message[]>([]);
  const [stage, setStage] = useState<"awaiting_member_id" | "awaiting_phone" | "verified" | "escalated">("awaiting_member_id");
  const [memberId, setMemberId] = useState<string | undefined>();
  const [failedAttempts, setFailedAttempts] = useState(0);
  const [toolCall, setToolCall] = useState<string | undefined>();
  const [hasStartedConversation, setHasStartedConversation] = useState(false);
  const [voiceSessionStarted, setVoiceSessionStarted] = useState(false);
  const [escalationReply, setEscalationReply] = useState<string | null>(null);
  const continueConversation = trpc.care.continueVerificationConversation.useMutation({
    onSuccess: result => {
      setConversation(current => [...current, { role: "assistant", content: result.reply }]);
      setMemberId(result.memberId);
      setFailedAttempts(result.failedAttempts);
      setToolCall(result.toolCall);
      if (result.stage === "escalated") {
        if (voiceSessionStarted) setEscalationReply(result.reply);
        else setStage("escalated");
        return;
      }
      setStage(result.stage);
      if (result.stage === "verified") window.setTimeout(onVerified, 700);
    },
  });
  const onSendMessage = (message: string) => {
    if (continueConversation.isPending || stage === "verified" || stage === "escalated") return;
    setConversation(current => [...current, { role: "user", content: message }]);
    if (!hasStartedConversation) setHasStartedConversation(true);
    continueConversation.mutate({ stage, message, memberId, failedAttempts });
  };
  const placeholder = stage === "awaiting_member_id" ? "Enter your member ID…" : "Enter your mobile number…";
  return <div className="min-h-screen bg-[#f7f3ec] text-[#191815]"><div className="mx-auto flex min-h-screen max-w-[1520px] flex-col px-5 py-5 sm:px-8 lg:px-12 lg:py-8"><header className="flex items-center justify-between border-b border-black/30 pb-5 text-[10px] font-semibold uppercase tracking-[0.18em] text-black/65"><div className="flex items-center gap-3"><HeartPulse className="size-4 text-[#005a48]" /> NovaCorp Health <span className="hidden text-black/35 sm:inline">/ Private care conversation</span></div><div className="flex items-center gap-2"><LockKeyhole className="size-3.5 text-[#005a48]" /> Protected care workspace</div></header><main className="grid flex-1 items-center gap-12 py-12 lg:grid-cols-12 lg:gap-10 lg:py-20"><section className="lg:col-span-7"><SectionLabel>Member services</SectionLabel><h1 className="mt-5 max-w-3xl font-editorial text-[clamp(3.5rem,8vw,8.4rem)] font-semibold leading-[0.84] tracking-[-0.065em]">Care that begins with <em className="font-normal text-[#005a48]">context.</em></h1><p className="mt-8 max-w-xl font-editorial text-2xl leading-8 text-black/62">A private conversation with Nova begins your care journey, then opens only the workspace associated with your verified member record.</p><div className="mt-10 grid max-w-xl grid-cols-3 gap-3 border-t border-black/20 pt-5 text-[10px] font-semibold uppercase tracking-[0.13em] text-black/52"><p>Conversation-led</p><p>Verified identity</p><p>Scoped records</p></div></section><section className="nova-panel bg-[#fcfaf6] lg:col-span-4 lg:col-start-9"><div className="flex items-start justify-between gap-4"><div><SectionLabel>Care assistant</SectionLabel><h2 className="mt-3 font-editorial text-4xl leading-none">Meet Nova.</h2></div><span className="grid size-10 place-items-center rounded-full bg-[#005a48] text-white"><Sparkles className="size-4" /></span></div><p className="mt-4 text-sm leading-6 text-black/60">Nova verifies both your member ID and the mobile number on your member record before opening a private workspace.</p><div className="mt-6">{stage === "escalated" ? <div role="status" className="border-l-2 border-[#005a48] bg-[#e7f1eb] p-5 text-sm leading-6 text-[#005a48]"><p className="font-editorial text-2xl text-[#191815]">Connecting you to a live agent.</p><p className="mt-2">Nova could not verify your member details after three attempts. Your secure verification session has ended, and a live agent can help with the next step.</p></div> : <AIChatBox messages={conversation} onSendMessage={onSendMessage} onVoiceAssistantMessage={content => setConversation(current => [...current, { role: "assistant", content }])} initialVoicePrompt={beginConversation.data?.reply} onVoiceSessionStart={() => { setHasStartedConversation(true); setVoiceSessionStarted(true); onVoiceSessionStart(); }} onVoiceSpeechComplete={content => { if (escalationReply && content === escalationReply) { (window as typeof window & { __novaHandsFreeSession?: boolean }).__novaHandsFreeSession = false; setEscalationReply(null); setStage("escalated"); } }} isLoading={continueConversation.isPending || beginConversation.isLoading || Boolean(escalationReply)} placeholder={placeholder} emptyStateMessage="Select Speak to Nova or enter your member ID to begin." height="350px" className="!rounded-none !border-black/25 !bg-transparent !shadow-none" />}</div>{toolCall && <p className="mt-4 flex items-center gap-2 border-t border-[#005a48]/20 pt-3 text-[10px] font-semibold uppercase tracking-[0.13em] text-[#005a48]"><ShieldCheck className="size-3.5" /> Secure operation complete · {toolCall}</p>}{continueConversation.error && <p role="alert" className="mt-4 border-l-2 border-red-800 pl-3 text-xs leading-5 text-red-800">{continueConversation.error.message}</p>}<p className="mt-5 text-[10px] leading-4 uppercase tracking-[0.12em] text-black/45">Your session automatically expires after 30 minutes of inactivity.</p></section></main><footer className="flex justify-between border-t border-black/30 py-5 text-[10px] uppercase tracking-[0.14em] text-black/45"><p>NovaCorp Health</p><p>Member care coordination</p></footer></div></div>;
}

export default function Home() {
  const [isVerified, setIsVerified] = useState(false);
  const workspaceQuery = trpc.care.getWorkspace.useQuery(undefined, { enabled: isVerified, retry: false });
  const signOutMutation = trpc.care.signOutPatient.useMutation({ onSuccess: () => { (window as typeof window & { __novaHandsFreeSession?: boolean }).__novaHandsFreeSession = false; setIsVerified(false); } });
  const [messages, setMessages] = useState<Message[]>([]);
  const [activities, setActivities] = useState<AgentActivity[]>([]);
  const [evidence, setEvidence] = useState<PolicyEvidence[]>([]);
  const [hasSearchedEvidence, setHasSearchedEvidence] = useState(false);
  const [bookingDraft, setBookingDraft] = useState<AppointmentSlot | undefined>();
  const [bookingConfirmation, setBookingConfirmation] = useState<BookingConfirmation | null>(null);
  const [bookingReviewOpen, setBookingReviewOpen] = useState(false);
  const [cancellationReviewOpen, setCancellationReviewOpen] = useState(false);
  const [cancellationMessage, setCancellationMessage] = useState<string | null>(null);
  const [coordinatorMode, setCoordinatorMode] = useState<"gemini" | "safe-fallback">("safe-fallback");
  const [isChatPending, setIsChatPending] = useState(false);
  const [chatError, setChatError] = useState<string | null>(null);

  const patient = workspaceQuery.data?.patient;
  const displayedActivities = activities.length > 0 ? activities : (workspaceQuery.data?.initialActivity ?? []);
  const displayedEvidence = evidence.length > 0 ? evidence : [];
  const bookingMutation = trpc.care.confirmBooking.useMutation({ onSuccess: result => { setBookingConfirmation(result); setBookingReviewOpen(false); setBookingDraft(undefined); workspaceQuery.refetch(); setMessages(current => [...current, { role: "assistant", content: `**Appointment confirmed**\n\n${result.clinician} · ${result.specialty}\n${result.dayLabel} at ${result.timeLabel}\n\nConfirmation: **${result.confirmationCode}**` }]); } });
  const cancellationMutation = trpc.care.confirmCancellation.useMutation({ onSuccess: result => { setCancellationReviewOpen(false); setCancellationMessage(`Cancellation confirmed for ${result.clinician} on ${result.dateLabel} at ${result.timeLabel}. Reference: ${result.confirmationCode}.`); workspaceQuery.refetch(); } });

  const sendChatMessage = (message: string) => {
    if (isChatPending) return;
    if (confirmsNoFurtherHelp(message)) {
      setMessages(current => [...current, { role: "user", content: message }, { role: "assistant", content: "Thank you for contacting NovaCorp Health. Your care session is now ending. Goodbye." }]);
      (window as typeof window & { __novaHandsFreeSession?: boolean }).__novaHandsFreeSession = false;
      window.setTimeout(() => signOutMutation.mutate(), 650);
      return;
    }
    setMessages(current => [...current, { role: "user", content: message }]); setChatError(null); setIsChatPending(true);
    const stream = new EventSource(`/api/novacorp/chat-stream?message=${encodeURIComponent(message)}`);
    stream.addEventListener("activity", event => setActivities(JSON.parse((event as MessageEvent<string>).data) as AgentActivity[]));
    stream.addEventListener("result", event => { const result = JSON.parse((event as MessageEvent<string>).data) as CoordinatorResult; stream.close(); setMessages(current => [...current, { role: "assistant", content: result.reply }]); setActivities(result.activities); setEvidence(result.evidence); setHasSearchedEvidence(true); setBookingDraft(result.bookingDraft); setCoordinatorMode(result.coordinatorMode); setIsChatPending(false); });
    stream.addEventListener("error", event => { stream.close(); let message = "The care coordinator could not complete this request."; try { const payload = JSON.parse((event as MessageEvent<string>).data); message = payload.error ?? payload.message ?? message; } catch { /* browser stream error */ } setChatError(message); setActivities([{ agent: "Coordinator", action: "Unable to respond", state: "error", detail: message }]); setIsChatPending(false); });
  };

  const greeting = useMemo(() => patient ? `Welcome back, ${patient.name.split(" ")[0]}.` : "Welcome back.", [patient]);
  if (!isVerified) return <MemberAccess onVerified={() => setIsVerified(true)} onVoiceSessionStart={() => { (window as typeof window & { __novaHandsFreeSession?: boolean }).__novaHandsFreeSession = true; }} />;
  if (workspaceQuery.isLoading) return <div className="min-h-screen bg-[#f7f3ec] p-6 lg:p-12"><Skeleton className="mx-auto h-[70vh] max-w-7xl rounded-none bg-black/10" /></div>;
  if (workspaceQuery.error || !patient) return <MemberAccess onVerified={() => { setIsVerified(false); workspaceQuery.refetch(); }} onVoiceSessionStart={() => { (window as typeof window & { __novaHandsFreeSession?: boolean }).__novaHandsFreeSession = true; }} />;

  const isWorking = isChatPending || bookingMutation.isPending || cancellationMutation.isPending;
  const upcoming = patient.upcomingAppointment;
  return <div className="min-h-screen overflow-x-hidden bg-[#f7f3ec] text-[#191815] selection:bg-[#d7e8dc] selection:text-[#191815]"><div className="mx-auto max-w-[1520px] px-5 py-5 sm:px-8 lg:px-12 lg:py-8"><header className="flex items-center justify-between gap-4 border-b border-black/30 pb-5"><div className="flex items-center gap-3 text-[10px] font-semibold uppercase tracking-[0.18em] text-black/65"><HeartPulse className="size-4 text-[#005a48]" /> NovaCorp Health <span className="hidden text-black/35 sm:inline">/ Member care workspace</span></div><Button variant="ghost" size="sm" onClick={() => signOutMutation.mutate()} className="rounded-none text-[10px] uppercase tracking-[0.13em] text-black/60 hover:bg-transparent hover:text-[#005a48]"><LogOut className="mr-1.5 size-3.5" />End session</Button></header><section className="grid gap-8 border-b border-black/30 py-10 lg:grid-cols-12 lg:gap-10 lg:py-14"><div className="lg:col-span-3 lg:pt-3"><SectionLabel>Member care · Verified</SectionLabel><p className="mt-4 max-w-[16rem] font-editorial text-xl leading-7 text-black/65">A complete view of the care journey.</p></div><div className="lg:col-span-6"><h1 className="font-editorial text-[clamp(3.35rem,8.5vw,8.35rem)] font-semibold leading-[0.83] tracking-[-0.065em]">Care, <em className="font-normal text-[#005a48]">connected.</em></h1></div><div className="flex flex-col justify-between lg:col-span-3 lg:pt-3"><p className="max-w-xs text-sm leading-6 text-black/62">A patient-scoped workspace for benefits, appointments, and evidence-led care coordination.</p><div className="mt-8 flex items-center gap-2 text-[10px] font-semibold uppercase tracking-[0.15em]"><Sparkles className="size-3 text-[#005a48]" /> Gemini-powered, guardrailed</div></div></section><main className="grid gap-8 py-9 lg:grid-cols-12 lg:gap-x-10 lg:gap-y-9"><section className="lg:col-span-7"><div className="flex items-end justify-between gap-5 border-b border-black/30 pb-4"><div><SectionLabel>Care assistant</SectionLabel><h2 className="mt-2 font-editorial text-[clamp(2rem,3vw,3rem)] leading-none">{greeting}</h2></div><Badge variant="outline" className={`mb-1 rounded-none border-black/25 px-2 py-1 text-[9px] font-semibold uppercase tracking-[0.14em] ${coordinatorMode === "gemini" ? "text-[#005a48]" : "text-black/55"}`}>{coordinatorMode === "gemini" ? "Gemini validated" : "Grounded mode"}</Badge></div><p className="mt-4 max-w-2xl font-editorial text-xl leading-7 text-black/62">Ask about your plan, copay, deductible, medication list, allergies, cited policy evidence, or appointment availability. Booking and cancellation always require your explicit confirmation; Nova does not provide diagnosis.</p><div className="mt-6"><AIChatBox messages={messages} onSendMessage={sendChatMessage} onVoiceAssistantMessage={content => setMessages(current => [...current, { role: "assistant", content }])} isLoading={isWorking} suggestedPrompts={suggestedPrompts} placeholder="Ask about benefits or appointments…" emptyStateMessage="Begin with a care question." height="510px" className="!rounded-none !border-black/30 !bg-[#fcfaf6] !shadow-none" /></div>{chatError && <p role="alert" className="mt-3 border-l-2 border-red-800 pl-3 text-xs leading-5 text-red-800">{chatError}</p>}</section><aside className="space-y-8 lg:col-span-5"><section className="nova-panel"><div className="flex items-start justify-between gap-5"><div><SectionLabel>Member profile</SectionLabel><h2 className="mt-2 font-editorial text-3xl">{patient.name}</h2><p className="mt-1 text-xs uppercase tracking-[0.14em] text-black/50">Member {patient.memberId}</p></div><span className="grid size-12 place-items-center rounded-full bg-[#005a48] font-editorial text-lg text-white">{patient.initials}</span></div><div className="mt-6 grid grid-cols-2 gap-x-5 gap-y-5 border-y border-black/15 py-5 text-sm"><div><p className="nova-label">Plan</p><p className="mt-1 font-editorial text-lg">{patient.plan}</p></div><div><p className="nova-label">Status</p><p className="mt-1 flex items-center gap-1.5 font-editorial text-lg"><span className="size-1.5 rounded-full bg-[#005a48]" /> {patient.planStatus}</p></div><div><p className="nova-label">Specialist copay</p><p className="mt-1 font-editorial text-lg">{patient.specialistCopay}</p></div><div><p className="nova-label">Deductible left</p><p className="mt-1 font-editorial text-lg">{patient.deductibleRemaining}</p></div></div><div className="mt-5 grid gap-5 sm:grid-cols-2"><div><p className="nova-label">Medications</p><ul className="mt-2 space-y-1.5 text-xs leading-5 text-black/70">{patient.medications.map(medication => <li key={medication.name}><strong className="text-black">{medication.name}</strong><br />{medication.dosage}</li>)}</ul></div><div><p className="nova-label">Allergies</p><div className="mt-2 flex flex-wrap gap-1.5">{patient.allergies.map(allergy => <Badge key={allergy} variant="outline" className="rounded-none border-[#b55239]/35 bg-[#fbf1e9] text-[10px] font-normal text-[#7d2c1d]">{allergy}</Badge>)}</div></div></div></section>{upcoming ? <section className="nova-panel bg-[#dcece4]"><div className="flex items-start justify-between"><div><SectionLabel>Upcoming appointment</SectionLabel><h2 className="mt-2 font-editorial text-2xl">{upcoming.clinician}</h2></div><Stethoscope className="size-5 text-[#005a48]" /></div><p className="mt-3 text-sm text-black/70">{upcoming.specialty}</p><div className="mt-5 flex flex-wrap gap-x-4 gap-y-2 border-y border-[#005a48]/20 py-3 text-xs text-black/70"><span className="flex items-center gap-1.5"><Clock3 className="size-3.5 text-[#005a48]" /> {upcoming.dateLabel} · {upcoming.timeLabel}</span><span className="flex items-center gap-1.5"><MapPin className="size-3.5 text-[#005a48]" /> Care center</span></div>{!cancellationReviewOpen && !cancellationMessage && <button onClick={() => setCancellationReviewOpen(true)} className="mt-4 text-[10px] font-semibold uppercase tracking-[0.14em] text-[#005a48] underline-offset-4 hover:underline">Manage appointment <ArrowUpRight className="ml-0.5 inline size-3" /></button>}{cancellationReviewOpen && <div className="mt-4 border-t border-[#005a48]/20 pt-4"><p className="text-xs leading-5 text-black/70">Cancelling is consequential. Confirm you want to cancel this exact appointment.</p><div className="mt-3 flex gap-2"><Button size="sm" onClick={() => cancellationMutation.mutate({ appointmentId: upcoming.id, confirmed: true })} disabled={isWorking} className="rounded-none bg-[#161513] text-[10px] uppercase tracking-[0.11em] hover:bg-[#b55239]">Confirm cancellation</Button><Button size="sm" variant="outline" onClick={() => setCancellationReviewOpen(false)} className="rounded-none border-black/25 bg-transparent text-[10px] uppercase tracking-[0.11em]">Keep it</Button></div>{cancellationMutation.error && <p role="alert" className="mt-2 text-xs text-red-800">{cancellationMutation.error.message}</p>}</div>}{cancellationMessage && <p className="mt-4 border-t border-[#005a48]/20 pt-4 text-xs leading-5 text-[#005a48]">{cancellationMessage}</p>}</section> : <section className="nova-panel bg-[#dcece4]"><SectionLabel>Appointments</SectionLabel><h2 className="mt-2 font-editorial text-2xl">Nothing upcoming</h2><p className="mt-3 text-sm leading-6 text-black/65">Ask the care assistant to search available appointments when you are ready.</p></section>}{bookingDraft && !bookingConfirmation && <section className="nova-panel border-[#b55239]/45 bg-[#fbf1e9]"><SectionLabel>Action requires confirmation</SectionLabel><h2 className="mt-2 font-editorial text-2xl">Review before booking</h2><div className="mt-5 border-y border-[#b55239]/25 py-4 text-sm leading-6"><p className="font-semibold">{bookingDraft.clinician} · {bookingDraft.specialty}</p><p>{bookingDraft.dayLabel} at {bookingDraft.timeLabel}</p><p className="mt-1 text-black/60">{bookingDraft.location}</p></div>{!bookingReviewOpen ? <div className="mt-5 flex flex-wrap gap-2"><Button onClick={() => setBookingReviewOpen(true)} className="rounded-none bg-[#161513] px-4 text-xs uppercase tracking-[0.12em] hover:bg-[#005a48]">Confirm booking <ChevronRight className="ml-1 size-3" /></Button><Button variant="outline" onClick={() => setBookingDraft(undefined)} className="rounded-none border-black/25 bg-transparent text-xs uppercase tracking-[0.12em]">Not now</Button></div> : <div className="mt-5 border-t border-[#b55239]/25 pt-4"><p className="text-xs leading-5 text-black/65">Selecting “Book now” submits this exact slot to the validated scheduling operation.</p><div className="mt-4 flex gap-2"><Button onClick={() => bookingMutation.mutate({ slotId: bookingDraft.id, confirmed: true })} disabled={isWorking} className="rounded-none bg-[#005a48] text-xs uppercase tracking-[0.12em] hover:bg-[#003d32]">Book now</Button><Button variant="outline" onClick={() => setBookingReviewOpen(false)} className="rounded-none border-black/25 bg-transparent text-xs uppercase tracking-[0.12em]">Go back</Button></div>{bookingMutation.error && <p role="alert" className="mt-3 text-xs text-red-800">{bookingMutation.error.message}</p>}</div>}</section>}{bookingConfirmation && <section className="nova-panel border-[#005a48]/40 bg-[#e7f1eb]"><SectionLabel>Appointment outcome</SectionLabel><h2 className="mt-2 font-editorial text-2xl">Booking confirmed</h2><p className="mt-4 text-sm leading-6"><strong>{bookingConfirmation.clinician}</strong> · {bookingConfirmation.specialty}<br />{bookingConfirmation.dayLabel} at {bookingConfirmation.timeLabel}</p><p className="mt-4 border-t border-[#005a48]/20 pt-3 text-[10px] uppercase tracking-[0.15em] text-[#005a48]">Confirmation {bookingConfirmation.confirmationCode}</p></section>}<EvidenceCard evidence={displayedEvidence} hasSearched={hasSearchedEvidence} /><section className="nova-panel"><div className="flex items-start justify-between"><div><SectionLabel>Live agent activity</SectionLabel><h2 className="mt-2 font-editorial text-2xl">Care orchestration</h2></div><ShieldCheck className="size-5 text-[#005a48]" /></div><div className="mt-6">{displayedActivities.map((item, index) => <ActivityItem key={`${item.agent}-${index}-${item.action}`} item={item} isLast={index === displayedActivities.length - 1} />)}</div></section></aside></main><footer className="flex flex-col justify-between gap-3 border-t border-black/30 py-5 text-[10px] uppercase tracking-[0.14em] text-black/45 sm:flex-row"><p>NovaCorp Health · Member care coordination</p><p>Patient-scoped access · Evidence-led responses</p></footer></div></div>;
}
